This repository contains 5 Python files which each display algorithms / topics that I found interesting relating to Cryptography.
The largest of these is Prime_Test where I have implemented numerous prime testing algorithms (both deterministic and probabilistic)
and timed how quick they can 'verify' primes. From this I created a plot demonstrating these results which can be seen in GraphPrimeTestSpeeds.
